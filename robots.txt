# robots.txt - Sleak Check Audit
# Reference: https://developers.google.com/search/docs/crawling-indexing/robots/intro

User-agent: *
Allow: /

# Point crawlers to the sitemap (update the domain if yours differs):
Sitemap: https://sleakcheckaudit.co.uk/sitemap.xml

# ---------------------------------------------------------------------------
# Notes (safe to delete):
# 1. Utility pages (success.html, sample-report.html) are NOT disallowed here;
#    they carry <meta name="robots" content="noindex"> instead. Blocking crawl
#    would stop Google seeing the noindex and can leave a bare URL in results.
# 2. Never block CSS/JS or /guides/ - Google renders pages before ranking them.
# 3. robots.txt is a crawl hint, not access control.
# 4. After launch: submit sitemap.xml in Google Search Console + Bing Webmaster.
# ---------------------------------------------------------------------------
